{
  "private": true,
  "name": "optimal-root",
  "scripts": {
    "prepare": "beemo create-config",
    "build": "packemon build --addEngines",
    "pack": "NODE_ENV=production packemon pack --addEngines --addExports --declaration=standard",
    "check": "yarn run type && yarn run test && yarn run lint",
    "clean": "packemon clean",
    "coverage": "yarn run test --coverage",
    "lint": "beemo eslint",
    "format": "beemo prettier",
    "release": "yarn run pack && yarn run check && cd optimal && npx np --yolo --no-yarn",
    "test": "beemo jest",
    "type": "beemo typescript --build"
  },
  "engines": {
    "node": ">=12.17.0",
    "npm": ">=6.13.0"
  },
  "devDependencies": {
    "@beemo/cli": "^2.0.1",
    "@beemo/core": "^2.0.2",
    "@beemo/dev": "^1.6.0",
    "@types/node": "^15.14.9",
    "packemon": "^1.4.2"
  },
  "workspaces": [
    "optimal/"
  ]
}
